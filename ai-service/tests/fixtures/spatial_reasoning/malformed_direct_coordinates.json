{
  "description": "Malformed: model emits direct absolute XYZ coordinates instead of a supported semantic spatial operation. The closed discriminated union has no such variant, so this must fail schema validation rather than being interpreted as a position.",
  "rawModelOutput": {
    "schemaVersion": 1,
    "target": {"kind": "object", "id": "object_sofa_123"},
    "intent": "spatial_domain",
    "summary": ["Move it to a specific point."],
    "geometry": {"mode": "preserve"},
    "material": {"mode": "preserve"},
    "spatial": {"mode": "replace", "spec": {"kind": "set_absolute_position", "x": 1.4, "y": 0.0, "z": 2.6}},
    "blockers": [],
    "assumptions": [],
    "reviewNotes": [],
    "confidence": 0.5
  }
}
